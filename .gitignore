# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
.venv/
venv/
env/
.env
*.egg-info/
dist/
build/
.ruff_cache/
.pyright/
.mypy_cache/

# Swift / Xcode
*.xcuserdata/
*.xcworkspace/xcuserdata/
build/
DerivedData/
.swiftpm/
*.xcarchive
*.dmg

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Models cache (HuggingFace)
models/
.cache/huggingface/

# Secrets (never commit)
server/.env
*.env
*.key

# Logs
*.log
CLAUDE.local.md

# Local secrets / setup state (never commit)
tmp/credentials.txt
tmp/client-config.txt

# /supabase-audit reports — operational signal, keep local-only
tmp/db-audit/

# Local debug screenshots (not part of the project)
tmp/mac-mini-screen-*.png

# Swift Package build artifacts and resolved deps
client/.build/
client/Package.resolved

# Claude Code internal state — narrow ignores so .claude/commands/*.md
# (slash command spec, expected to be checked-in by OVERVIEW.md + CLAUDE.md)
# remain tracked.
.claude/scheduled_tasks.lock
.claude/settings.local.json
.claude/cache/
.claude/sessions/
